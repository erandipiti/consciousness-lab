# protocols/

Session protocol definitions and operator-facing materials for Study 001.

**Empty at CL-001.** The Phase 0 protocol has not been supplied — see
[`../docs/PHASE0_PROTOCOL.md`](../docs/PHASE0_PROTOCOL.md) for what must arrive
before anything lands here.

This directory is not importable Python and the package must not read from it
implicitly (`../docs/ARCHITECTURE.md`). How protocols are versioned, and how a
session records which protocol version it ran under, are open questions.
